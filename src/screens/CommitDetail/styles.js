import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    main: {
        marginVertical: 10,
        padding: 10,
    },
    fileView: {
        backgroundColor: '#FFF',
        marginBottom: 10,
        borderWidth: 1,
        borderRadius: 5,
        justifyContent: 'center',
    },
    fileText: {
        color: '#000',
        fontSize: 16,
        padding: 10,
        justifyContent: 'center',
    }
})